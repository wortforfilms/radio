# Troubleshooting

- Parser returns `None`: email did not contain credit keywords, amount, or UTR.
- Match returns `blocked`: amount/order match is ambiguous or missing.
- Poller returns `blocked`: credentials are NULL.

